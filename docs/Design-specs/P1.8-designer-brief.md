# P1.8 Designer Brief — Delta from the 3 new PNGs

**Context:** You have `Sequence-reroot.png`, `New-Camera-flow-plan.png`, `Unsequenced-branch.png` — these nail `Camera-flow-specs.md` core (§4 graph vs sequence, §9/§13 re-root, §8 branch, §20 vis). Brief below is only **what to fix / what to redo / what's missing** — no repeat of what already works.

**Source of truth:** `Camera-flow-specs.md` (amended docs: `Design-specs.md` free-camera → unsequenced, `Design-shell-specs.md` §9 free-camera → unsequenced distinction — done).

---

## 1. Keep — minor convention only — docs marked, sketch not edited

Per request: these 7 are structurally correct — **do not edit the PNGs**. Convention delta is recorded in docs (`Camera-flow-specs.md §2` + `Shell §4` are canonical). `P3.1` QA logs deviation, no PNG patch.

| PNG | Docs note where referenced (sketch kept as-is) |
|---|---|
| **Sequence-reroot.png** (new) | `Design-shell-specs.md §4` canonical `A — B` (no arrows) — sketch shows `A → B` in sidebar + `Measure` in toolbar (`Shell §16` — Measure is Scene Plan Layout only) + `Yaw/Y` editable in Plan inspector (spec: `Y preserved` read-only, `Orientation — in Plan` §22). Marked in `P3.1` mapping as `known deviation — keep`. |
| **New-Camera-flow-plan.png** (new) | Same as above. Also `Shell §9 Timing` — sketch omits per-direction `Duration A→B 4.2s / B→A 3.5s + Speed` readout/label (shipped P1.5) — marked as `P1.5 gap, docs are truth`. |
| **Unsequenced-branch.png** (new) | Same `→` → `—` note only. Reference legend (`blue thick = Sequence / gray thin = other graph / blue ① = sequenced / green ◯ = unsequenced`) is canonical — keep. |
| **Camera-2D.png** | `Camera-flow-specs.md §2` `Free Cameras → Unsequenced`, `Not in order yet → Connected to 1` — marked in `P3.1` mapping. Sketch also shows `Height Y 4.20` input vs docs `Y preserved` read-only (`Shell §9`). |
| **Camera-3D.png** | Same `Free → Unsequenced` mark only. Toolbar `Select/Move/Rotate/Add Camera/Path/Frame/View` correct (`Shell §10`) — no note. |
| **Camera-sequence.png** | Same `Free → Unsequenced` mark — consolidate with `Camera-3D.png` in `P3.1` mapping (duplicate view). |
| **Timeline-expanded.png** | Same `Free → Unsequenced` mark. Lanes `Camera Path / Shots / FOV / Look At / Roll` keep (`Shell §12`). |

Consistent tokens per `Design-specs.md §7` — `blue #2F8CFF` numbered `①②③` sequenced, `green #31C985` ring `◯` unsequenced (already renamed `unsequenced ring` in docs). Sketches keep old rendering; docs are truth.

---

## 2. Redesign — model mismatch, not just rename

Archive these as alt concepts; do not ship as P1.8 truth.

* **`Camera-sidebar.png`** — **Full redo.** Left panel is `Tour / Lighting/Time/Weather / Sequence 8 shots 01-08 + timecodes 00:00-00:06 + 24mm / Free Cameras 4 cameras / Connections Walk 4.3s`. Spec sidebar is `Environment / Sequence Inspector / Unsequenced / Connections` (`Shell §4`). Also `Aperture/DOF f/4.0` not in spec. Replace left panel, keep right inspector framing envelope if desired.
* **`Collapsed-camera.png`** — **Full redo.** Filename promises `48px` collapsed Camera Timeline (`Shell §12`) but image duplicates framing viewport `Shot 03 Central Hall` (same as `Framing-Authoring-3D.png`). Need true **Camera Plan** collapsed strip: `Tour selector + Play/Pause/Follow/Recenter/Stop + Snap + 00:00/01:20 + Zoom + Collapse chevron`, lanes hidden.
* **`Framing-Authoring-3D.png`** — **Partial redo (left 30%).** Viewport `Focus Target / Safe Frame 90% / Rule of Thirds / Subject Frame` + Inspector `Framing Envelope 14%→86% / Focus Timing Early/Center/Late / Lens 24/35/50/85 / Parallax Warning` are correct `P1.6`. Left `Shots 01-08` list must become `Sequence Inspector ①②③④ + Unsequenced + Connections` (`Shell §4/10` — same sidebar in Plan and 3D). Keep right 70%.

---

## 3. Missing canvases — to cover full flow spec (needed for P1.8 acceptance)

None of the 10 PNGs show these § states. Small panels or add-on frames, not full redesigns:

* **§6 Empty Sequence** — `SEQUENCE / Drop a camera here` empty state with `UNSEQUENCED ◯ A ◯ B ◯ C` pool. Note `P1.8 D4` keeps `≥2` nodes (empty/one-node is intermediate, existing `No camera flow` copy covers it) — still need visual.
* **§7+§11 Insert zones** — drag `Unsequenced X` into gap `B — C` requires **both** `B—X` and `X—C`. Show valid drop `B | X | C` vs invalid single-edge `B—X` only, plus `Start/End are always valid` zones.
* **§12 Remove middle failure** — `Cannot remove Camera B from Sequence / Camera A has no connection to Camera C` (spec copy). Head/Tail removal always valid vs middle guarded — not shown (only move failure is shown).
* **§16/§17 Delete protection** — `Cannot delete connection / Sequence requires movement between Camera A and Camera B` + middle node delete blocked when `A—C` missing. No panel shows this.
* **§18 Preview** — `Preview Camera` (context action on `①` and `◯` rows + Inspector + Plan `→ switch to 3D preserve selection`) and `Preview Connection` — `P1.8 §4` wiring, zero PNG shows button.
* **§15 Path edit keeps sequence** — already hinted by curved `B—C` with 2 anchors, but annotate `Sequence B→C unchanged, geometry bends` + `Duration stays 4.2s, speed updates`.
* **§19 Timeline = Sequence only** — add note `Unsequenced never appears as normal stop` (the green end-pill in `Camera-2D.png`/`Timeline-expanded.png` is branch presentation only — clarify or hide).

Copy must be exact spec wording — `No connection between Camera A and Camera C`, `Sequence requires movement between…`, `Preview Camera`.

---

**Deliverable priority:** 1) **Keep 7 as-is** — docs marked above, no PNG re-export; P3.1 logs `→`/`Free`/`Y` as known deviation, docs are truth 2) Redraw 3 mismatched left panels/collapsed strip 3) Add 5 missing small panels (empty/insert/remove/delete/preview). No new tokens, no behavior change. Sketches not edited per request.
