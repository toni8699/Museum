# P1.8 Designer Brief — Delta from the 3 new PNGs

**Context:** You have `Sequence-reroot.png`, `New-Camera-flow-plan.png`, `Unsequenced-branch.png` — these nail `Camera-flow-specs.md` core (§4 graph vs sequence, §9/§13 re-root, §8 branch, §20 vis). Brief below is only **what to fix / what to redo / what's missing** — no repeat of what already works.

**Source of truth:** `Camera-flow-specs.md` (amended docs: `Design-specs.md` free-camera → unsequenced, `Shell-camera-workspaces.md` §9 free-camera → unsequenced distinction — done).

---

## 1. Keep — minor convention only — docs marked, sketch not edited

Per request: these 7 are structurally correct — **do not edit the PNGs**. Convention delta is recorded in docs (`Camera-flow-specs.md §2` + `Shell §4` are canonical). `P3.1` QA logs deviation, no PNG patch.

| PNG | Docs note where referenced (sketch kept as-is) |
|---|---|
| **Sequence-reroot.png** (new) | `Design-shell-specs.md §4` canonical `A — B` (no arrows) — sketch shows `A → B` in sidebar + `Measure` in toolbar (`Shell §16` — Measure is Scene Plan Layout only) + `Yaw/Y` editable in Plan inspector (spec: `Y preserved` read-only, `Orientation — in Plan` §22). Marked in `P3.1` mapping as `known deviation — keep`. |
| **New-Camera-flow-plan.png** (new) | Same as above. Also `Shell-camera-workspaces.md §9 Timing` — sketch omits per-direction `Duration A→B 4.2s / B→A 3.5s + Speed` readout/label (shipped P1.5) — marked as `P1.5 gap, docs are truth`. |
| **Unsequenced-branch.png** (new) | Same `→` → `—` note only. Reference legend (`blue thick = Sequence / gray thin = other graph / blue ① = sequenced / green ◯ = unsequenced`) is canonical — keep. |
| **Camera-2D.png** | `Camera-flow-specs.md §2` `Free Cameras → Unsequenced`, `Not in order yet → Connected to 1` — marked in `P3.1` mapping. Sketch also shows `Height Y 4.20` input vs docs `Y preserved` read-only (`Shell-camera-workspaces.md §9`). |
| **Camera-3D.png** | Same `Free → Unsequenced` mark only. Toolbar `Select/Move/Rotate/Add Camera/Path/Frame/View` correct (`Shell §10`) — no note. |
| **Camera-sequence.png** | Same `Free → Unsequenced` mark — consolidate with `Camera-3D.png` in `P3.1` mapping (duplicate view). |
| **Timeline-expanded.png** | Same `Free → Unsequenced` mark. Lanes `Camera Path / Shots / FOV / Look At / Roll` keep (`Shell §12`). |

Consistent tokens per `Design-specs.md §7` — `blue #2F8CFF` numbered `①②③` sequenced, `green #31C985` ring `◯` unsequenced (already renamed `unsequenced ring` in docs). Sketches keep old rendering; docs are truth.

---

## 2. Redesign — model mismatch, not just rename

Archive old files as alt concepts; do not ship as P1.8 truth. **Delivered 2026-08-21 — superseded by new PNGs; old files kept as alt concepts only.**

* **`Camera-sidebar.png`** — **Delivered as `Design-png/Camera/camera-sidebar.png` + `Side-bar.png` (2026-08-21).** Old left panel `Tour / Lighting/Time/Weather / Sequence 8 shots 01-08 + timecodes 00:00-00:06 + 24mm / Free Cameras 4 cameras / Connections Walk 4.3s` replaced with canonical `Environment / Sequence Inspector / Unsequenced / Connections` (`Shell §4`). `Aperture/DOF f/4.0` removed. Old `Camera-sidebar.png` (capital C) archived; new lowercase `camera-sidebar.png` + `Side-bar.png` are ground truth (case-alias — `git mv` to normalize).
* **`Collapsed-camera.png`** — **Delivered as `Design-png/Camera/camera-timeline-collapsed.png` (2026-08-21).** Old image duplicated framing viewport `Shot 03 Central Hall`; new shows true `48px` collapsed strip: `Tour selector + Play/Pause/Follow/Recenter/Stop + Snap + 00:00/01:20 + Zoom + Collapse chevron`, lanes hidden per `Shell §12` (`Design-specs §16`). Old `Collapsed-camera.png` missing on disk — plan ref updated.
* **`Framing-Authoring-3D.png`** — **Delivered as `Design-png/Camera/Camera-3D-Framing-new.png` (2026-08-21).** Viewport `Focus Target / Safe Frame 90% / Rule of Thirds / Subject Frame` + Inspector `Framing Envelope 14%→86% / Focus Timing Early/Center/Late / Lens 24/35/50/85 / Parallax Warning` kept (`P1.6` correct); left `Shots 01-08` replaced with canonical 4-section sidebar (`Shell §4/10` — same sidebar in Plan and 3D). Expanded timeline now correctly shows 5 lanes via `Camera-3D-timeline-expanded.png` (see §3). Old `Framing-Authoring-3D.png` archived as alt concept.

---

## 3. Missing canvases — to cover full flow spec (needed for P1.8 acceptance)

**Update 2026-08-21:** 6 of 7 now delivered via Aug 21 batch; 1 remains (insert zones). Small panels or add-on frames, not full redesigns:

* **§6 Empty Sequence** — **Delivered as `Design-png/Camera/Side-bar.png` (2026-08-21).** `SEQUENCE / Drop a camera here` empty state with `UNSEQUENCED ◯ A ◯ B ◯ C` pool. Note `P1.8 D4` keeps `≥2` nodes (empty/one-node is intermediate, existing `No camera flow` copy covers it).
* **§7+§11 Insert zones** — **Still missing.** Drag `Unsequenced X` into gap `B — C` requires **both** `B—X` and `X—C`. Show valid drop `B | X | C` vs invalid single-edge `B—X` only, plus `Start/End are always valid` zones. No PNG yet covers this.
* **§12 Remove middle failure** — **Delivered as `Design-png/Camera/Remove-from-sequence.png` + `Remove-sequence.png` + `Unsequence-Sequenced.png` (2026-08-21).** `Cannot remove Camera B from Sequence / Camera A has no connection to Camera C` (spec copy). Head/Tail removal always valid vs middle guarded.
* **§16/§17 Delete protection** — **Delivered via same `Remove-*.png` set (2026-08-21).** `Cannot delete connection / Sequence requires movement between Camera A and Camera B` + middle node delete blocked when `A—C` missing.
* **§18 Preview** — **Delivered as `Design-png/Camera/Camera-preview-connection.png` (2026-08-21) + `Camera-3D-timeline-expanded.png` Inspector `Preview Transition ▶` and context actions.** `Preview Camera` (context action on `①` and `◯` rows + Inspector + Plan `→ switch to 3D preserve selection`) and `Preview Connection` — `P1.8 §4` wiring.
* **§15 Path edit keeps sequence** — **Delivered as `Design-png/Camera/Path-edit.png` (2026-08-21).** Curved `B—C` with anchors annotated `Sequence B→C unchanged, geometry bends` + `Duration stays 4.2s, speed updates` + fallback derived duration.
* **§19 Timeline = Sequence only** — **Delivered as `Design-png/Camera/Timeline-sequence-only.png` + `Camera-3D-timeline-expanded.png` (2026-08-21).** Legend `TIMELINE (Sequence only) Shows ordered traversal. Does not include unsequenced nodes.` + `GRAPH TOPOLOGY — Sequence Path (B → C → D) / Branch Connection (Unsequenced)` + `SEQUENCE MODEL` box (`Timeline shows Sequence only. Unsequenced cameras remain connected.`). Green end-pill in `Camera-2D.png`/`Timeline-expanded.png` is branch presentation only — clarified.

Additionally delivered 2026-08-21 (beyond §3 scope): `Camera-3D-new.png` (framing update), `Camera-3D-Framing-new.png` (§2 left redo), `Camera-3D-timeline-expanded.png` (canonical 5-lane `Camera Path / Shots / FOV / Look At / Roll` per `Shell §12` / `Design-specs §24`, with branch `Camera E` pill, timing `B—C 4.2s / C—D 5.1s`, derived speed).

Copy must be exact spec wording — `No connection between Camera A and Camera C`, `Sequence requires movement between…`, `Preview Camera`.

---

**Deliverable priority (updated 2026-08-21):** 1) **Keep 7 as-is** — docs marked above, no PNG re-export; P3.1 logs `→`/`Free`/`Y` as known deviation, docs are truth 2) **3 redos delivered** — `camera-sidebar.png`/`Side-bar.png`, `camera-timeline-collapsed.png`, `Camera-3D-Framing-new.png` + `Camera-3D-timeline-expanded.png` (5-lane canonical `Shell §12`) — old files archived as alt concepts 3) **6 of 7 missing canvases delivered** — only `§7+§11 Insert zones` remains; add `B | X | C` valid vs single-edge invalid + Start/End zones panel. No new tokens, no behavior change.
