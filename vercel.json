{
  "framework": "sveltekit",
  "buildCommand": "npm run build -w @portfolio/editor",
  "outputDirectory": "apps/editor/.vercel/output",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://biskiq-api.onrender.com/:path*" }
  ]
}
