# P21.5 Plan: UI Polish Pass — Shell Ergonomics, Button Semantics, Inspector Density & Viewport De-Cluttering

**Status:** Consolidated implementation spec (codebase-verified 2026-09-05 — designer draft reconciled against source; answers the draft §5 inline).
**Authority:** `docs/plans/2026-09-04-P21-unified-project-shell-spatial-reconciliation.md` §P21.5 + `docs/Design-specs/Design-Plan(P21+).md` + `docs/components/shell.md` (P21+ target) + P12 timeline contract. Behavior-beats-pixels per P21 §1: no PNG redefines selection/history/ownership/transforms/topology/timing/scopes/isolation.
**Scope:** Presentation-only. No behavior, ownership, transform-model, topology, timing, persistence, isolation, or navigation change. No new controls, no dead buttons.

> Draft-header correction: there is no `theme.md`. Theme authority is `apps/editor/src/lib/editor/theme.svelte.ts` + `styles/tokens.css` (header comment = layer contract) + `styles/scene-palette.ts` (Three.js mirror, contract-tested).

---

## 1. What the draft got right, and what drifted

Verified against source (file:line citations are the buildable truth; paths below are the corrected ones):

1. **Outline disease — directionally correct, tokens already partway there.** `styles/controls.css` already defines ghost/active segmented tokens; `WorkspaceRibbon.svelte:63-64` already uses ghost buttons with `.active` fill. Remaining work is enforcement + per-theme alignment (see Slice 1.3), not greenfield grammar.
2. **Segmented controls — half done.** `[Scene|Camera]` + `[Plan|3D]` are already enclosed `.segmented` groups in `app/WorkspaceRibbon.svelte:30-41`. Only `[Layout|Arrange]` is still a loose `.tool-group.mode-group` in `layout/LayoutDraftToolbar.svelte:64-67` (ribbon mode additionally strips containers at `:115`). Slice 1.2 narrows to unifying that third switch.
3. **Row geometry — already locked, verify-only.** `--editor-project-row-height:36px / --editor-ribbon-height:32px / --editor-status-height:24px` are set in `styles/tokens.css:510-513` (`.project-editor` override; `:root:195-196` still reads `56px/32px` as pre-P21 base), consumed by `app/EditorApp.svelte:2037` grid rows and enforced for status by `styles/editor-shell.css:26`. Row 1 owner is `app/ProjectRow.svelte:205` (inner controls 28px at `:208`), Row 2 is `app/WorkspaceRibbon.svelte:60` (buttons currently **26px** at `:63` — micro-tune to the 28px Row 2 control spec, see Slice 1.1). There is no `app/EditorAppBar.svelte`; the legacy bar lives at `lib/editor/EditorAppBar.svelte` pending P21.1 split-then-delete.
4. **Viewport pills — files misidentified, class misnamed.** No `instruction-banner` exists anywhere in `apps/editor/src`. The floating pills are `.plan-help` (`role="status"`, pill styling) in **two** viewports: `camera-plan/CameraPlanViewport.svelte:172` (string) rendered at `:808`, and `layout/LayoutPlanViewport.svelte:1715-1728`. `app/PlanWorkspace.svelte` (draft said `plan/PlanWorkspace.svelte` — wrong directory) is **not** a banner source. Fix both `.plan-help` sites; leave `PlanWorkspace` alone unless QA proves otherwise.
5. **Status bar hints — already exist.** `app/StatusBar.svelte:132-142` already renders Plan (`Middle+Drag pan / Scroll zoom / Shift angle snap`) vs 3D (`Alt+Drag orbit / Shift+Drag pan / Scroll zoom`) hints, plus P21.2/P21.3 per-workspace strings at `:108-120` and left/save/right zones at `:124-147`. Slice 2.2 is keep + lock 24px, not add.
6. **Inspector — structure confirmed, density claim overstated.** `EditorInspector.svelte` lives at `lib/editor/EditorInspector.svelte` (not `app/`), 1247 lines, delegating to `camera/EditorCameraInspector.svelte`, `app/CameraPlanInspector.svelte`, `EditorLight/Material/Placement/Primitive/TransformInspector` + `fields/EditorNumberField.svelte` (`:8-16`), with routing at `:652` (layout) / `:837` (staging) / `:896` (assets) / `:939` (camera) / `:945` (placement) / `:1043` (empty). Permanent panels confirmed at `:1051-1067` (Camera: Pan/Grid/Floor, hidden only in staging) and `:1069-1083` (Lighting: Bright/Visitor + Ambient/Directional/Fog sliders, unconditional). Transform is **already a 3-column grid** (`EditorTransformInspector.svelte:107-126` Position, `:131-153` Rotation, `:223-251` independent Scale via `.field-grid` at `:314-318`) — the draft's "three stacked full-width inputs" is wrong. Real work: compact `EditorNumberField` density + axis chips + tabular-nums (Slice 3.1). **Do not create `components/TransformInputRow.svelte`** (it does not exist; a new component duplicates the shared field). There is no `NumericInput.svelte`; the shared field is `fields/EditorNumberField.svelte` (+ sibling `fields/EditorVec3Field.svelte`).
7. **Light theme — already exists, scope to its block.** `porcelain-atelier` is the shipped first light identity (`tokens.css:374-418`: terracotta `#d94826`, `accent-soft 12%`, borders `#ddd6cb/#c4bbb0`, `color-scheme:light`). The draft's "de-saturate + quiet hairlines" is verify + micro-tune **inside that block only**. Never hardcode `rgba(217,72,38,0.10)` / `#B43B1E` globally — every other theme is dark with its own accent family (navy `#2F8CFF` invariant for selection). `theme.svelte.ts` lives at `lib/editor/theme.svelte.ts` (not `styles/`).
8. **Timeline mini-player — draft Slice 5.1 contradicts P12/P21, must be rewritten.** Reality (`camera/EditorCameraTimelineFrame.svelte`): collapsed **48px** floating mini-player (`:744-753` absolute `bottom:16px` centered pill, `border: --editor-border-normal`, **no red/coral border anywhere**), expanded header **36px** (`:782-789`), transport = scope-capsule + edge-flip + prev/play/next + scrubber + POV/Observer + observer actions (`:684-721`). P21 umbrella §9 + `components/shell.md` pin this: keep transport **above the lanes**, one shared dock geometry, 48px mini-player, lane density `label 120px / ruler 28px / lanes 44/48/34/34/32`, `+View Key` 3D-Sequence only. The draft's "dock into status dock as 40px toolbar, strip red border, ⏮▶⏭ 24px ghosts" would regress P12. Slice 5 below replaces it with density-only polish.
9. **`Tour` is not stray — do not delete.** `LayoutDraftToolbar.svelte:90-92` `Tour` toggles `planView.showTourOverlay` in Layout mode only. It is a legitimate Plan overlay option in the P21 Row 2 capability matrix (Zone C precision/overlay), not authoring-context leakage. Reference-PNG omission is generator drift per P21 §1. Default: keep. Removal requires an owner decision, not a polish side effect.

---

## 2. Execution slices (consolidated)

```text
P21.5 Polish Execution (presentation-only)
├─ Slice 1: Row geometry verify + Layout|Arrange segmented unification + per-theme button grammar
├─ Slice 2: .plan-help removal (2 viewports) + status-bar lock + gizmo single-ending (§2.4, pinned) + edge timing pill (§2.5)
├─ Slice 3: Inspector density via EditorNumberField + selection isolation to View menu
├─ Slice 4: Porcelain-block micro-tune + Inspector type grammar + tabular-nums
└─ Slice 5: Timeline density only (P12 geometry preserved)
```

### Slice 1: Rows, segmented controls, button grammar

#### 1.1 Geometry — verify, then one micro-tune
* Row 1 = `app/ProjectRow.svelte` (36px via token), Row 2 = `app/WorkspaceRibbon.svelte` (32px), status = `app/StatusBar.svelte` (24px token + `editor-shell.css:26` enforcement). Confirm at runtime: 36 / 32 / 24 with no residual 44px layout shell-bar.
* Micro-tune: `WorkspaceRibbon.svelte:63` buttons read `height:26px; padding:0 7px` — align Row 2 controls to the P21.1 **28px** control height (`controls.css:11` `--editor-control-height:28px`) unless visual QA shows a 32px-ribbon reason to keep 26px. Segmented inner buttons may stay 24px inside the 28px container per the draft's container CSS.
* Hairlines: Row 1 / Row 2 bottom borders use `var(--editor-border-subtle)`; ribbon currently uses `--editor-border-normal` at `WorkspaceRibbon.svelte:60` — reconcile to subtle per spec.

#### 1.2 Segmented controls — unify the third switch only
* Keep `WorkspaceRibbon.svelte:30-41` Zone A groups as-is (fixed `flex:0 0 240px` at `:61`).
* Bring `LayoutDraftToolbar.svelte:64-67` `[Layout|Arrange]` into the same `.segmented` grammar (container + `aria-pressed`/`.active` + 24px inner buttons) so all three switches — `[Scene|Camera]`, `[Plan|3D]`, `[Layout|Arrange]` (Scene Plan only) — share one visual language. Preserve the ribbon-mode container strip (`:115-118`) semantics: the **Ribbon** provides the enclosure in ribbon mode; the floating toolbar keeps its own enclosure when floating.
* Accessibility: keep `role="group"` + `aria-label` on each group; keep `aria-pressed` (current pattern) — do not bulk-rename to `aria-selected` unless the design-system audit blesses it.

#### 1.3 Button grammar — enforce per-theme, no global terracotta
* Ghost default (transparent, `text-secondary`, no accent border) for inactive tools; hover → `var(--editor-bg-hover)` + `text-primary`; active → theme accent-soft fill + accent-border + primary text (current ribbon active at `WorkspaceRibbon.svelte:64` uses `bg-control + accent text` — align to the grammar without breaking dark themes); primary CTA (`▶ Preview`, `Save`) → solid `var(--editor-accent)` with high-contrast text.
* Draft's `#B43B1E` sienna text and `rgba(217,72,38,0.10)` fill are **porcelain-block values only** if QA needs them — never global tokens. Selection blue (`#2F8CFF` family, axis/scene-palette invariants) stays untouched in every theme.

### Slice 2: Canvas de-cluttering, status bar, gizmo

#### 2.1 Remove `.plan-help` pills from both viewports
* Delete or gate behind a dismissible first-run affordance (owner call; default delete): `camera-plan/CameraPlanViewport.svelte:808` (+ string logic `:161-173`) and `layout/LayoutPlanViewport.svelte:1715-1728`. No `instruction-banner` search-and-destroy — the class does not exist. Leave `app/PlanWorkspace.svelte` and `app/CameraPlanWorkspace.svelte` shells untouched.

#### 2.2 Status bar — lock, don't build
* Content already correct (`StatusBar.svelte:108-147`). Work: enforce exactly **24px** (component currently declares `min-height:1.7rem` at `:155` — the shell override holds it at 24px; reconcile so the component is correct standalone), keep center hints hidden `<62rem` (`:186-188`), keep `Metric (m)` right zone. **Status bar stays informational** (its own header comment forbids authoring actions) — hints and strings only.

#### 2.3 `Tour` — keep (reverses draft §2.3)
* `Tour` (`LayoutDraftToolbar.svelte:91`) stays as the Layout-mode Plan overlay toggle. Any removal is a P21-capability decision for the owner, not polish. Verify it renders only in `layout` plan mode (it does — `:90` guard) and never in Arrange/Camera.

#### 2.4 Gizmo single-ending — pinned, with corrections (only slice that touches gizmo geometry)
* **Problem:** three.js `TransformControls` (`three@^0.175.0` per `apps/editor/package.json` — caret, so a minor bump needs a visual re-check) decorates **both** ends of Move/Scale axes; the negative-end arrowhead/grip adds ink without capability.
* **Fix:** hide the negative-end tip mesh per axis (Move arrow / Scale grip at the `+` tip stays), once, synchronously beside `applyEditorGizmoPalette(transformHelper)` in `gizmo/EditorTransformControlsHost.svelte:38-42` (module-init code, not a mount effect — the walker runs in the same synchronous init path). Match tip meshes by local-position sign, skip axis lines at origin, **never touch the invisible `picker` hierarchy** (hit-testing/snap/hover/active untouched — host comment `:39-40` already pins this contract).
* **Home:** walker lives in `styles/scene-palette.ts` beside `applyEditorGizmoPalette` (same cosmetic-only contract: materials/visibility only, geometry/pickers untouched; keeps the `scene-palette.test.ts` token-sync story intact).
* **Overlap:** additive only — P21.2 keeps the gizmo untouched, P3.2 recolored materials only. Applies to every context of the shared host (Scene, Layout candidate session, camera-handle translate) automatically. Test: unit asserting all axes remain present and draggable (presence, not pixels) + existing gizmo/selection suites stay green.

#### 2.5 Compact camera edge timing pill (replaces the stacked debug strings)
* **Home (corrected):** the text is composed in `layout/plan-camera-projection.ts:181-196` (`buildPlanCameraAuthoringProjection` emits two `camera-timing-label` text primitives per edge — `${fromLabel}→${toLabel} ${dur}s[ auto]` at midpoint offsets `[0,-16]` / `[0,18]`), rendered as bare 10px SVG text by `layout/PlanSvg.svelte:277`. `camera-plan/CameraPlanViewport.svelte` only renders primitives — it owns no label strings, so the fix lands in the projection + renderer, not the viewport.
* **Remove:** full node-name strings, the `auto` word, and the stacked two-line layout (both offsets collapse to a single midpoint anchor `[0,0]`).
* **Replace with one pill per connection** (single primitive, centered on the spline midpoint):
  * Content (durations only, `toFixed(1)` + `s` as today — Image 8's `4.00 s` spacing is generator detail): equal directions → `4.0s`; differing → `4.2s · 3.8s` (forward · reverse); non-finite stays `—` (current fallback preserved).
  * Authored vs automatic stays distinguishable per the `camera-tour.md` contract, but through a visual channel instead of the word `auto`: automatic segments render muted with a dotted underline + tooltip (`automatic — derived from path length and default speed`); authored segments render solid. Node names, direction breakdown, and derived speed live only in the connection Inspector (Slice 3.2; `CameraPlanInspector.svelte:278-279` already shows From/To).
  * Styling: ~18px height, `radius 4px`, 11px tabular text, neutral bg + muted/dark text by default; selected edge → `var(--editor-accent)` fill + white text (Image 8's `[ 4.00 s ]` chip). `pointer-events:none` preserved.
* **Plumbing:** label primitives carry no selection/authored state today (only the sibling `connection.selected` does) — extend the primitive (selected flag + per-segment authored flags) and teach the renderer to draw the rounded-rect badge behind centered text (`PlanSvg` text primitives are bare `<text>` today). No new selection, timing, or motion logic; `resolveCameraConnectionTiming` and per-direction Inspector authoring are untouched.
* **Pinned amendments (same slice, presentation-only):** update `docs/components/camera-tour.md:90-91` (both-directions labeling → compact pill grammar) and `tests/lib/editor/layout/plan-camera-projection.test.ts:298-310` (two verbose strings → single-pill expectations). Without these the slice contradicts a contract line and fails a pinned test.

### Slice 3: Inspector density + selection isolation (no new row component)

#### 3.1 Compact XYZ — restyle the shared field, don't fork it
* Reuse `fields/EditorNumberField.svelte` (+ `EditorVec3Field` where used): compact **28px** row variant with inline axis chip (18×18px, 10px/700, radius 3px; X `rgba(240,82,82,.15)/#F05252`, Y `rgba(69,200,120,.15)/#45C878`, Z `rgba(59,130,246,.15)/#3B82F6` — consistent with `--editor-axis-*`), borderless tabular input (`font-variant-numeric:tabular-nums; font-size:12px`).
* Apply via the existing `.field-grid` 3-column rows in `EditorTransformInspector.svelte` (Position/Rotation/independent Scale) and the layout X/Z/Yaw rows in `EditorInspector.svelte:719,725,731` + `:865,871,877`. Fold the standalone `.axis-legend` (`EditorTransformInspector.svelte:99-103`) into the per-field chips so the legend doesn't consume a second row. Keep uniform/independent scale toggle semantics (placement contract) — density only.
* Raw `<input type="number">` sites (layout metrics/openings/room fields, e.g. `EditorInspector.svelte:747-752`) take the same input typography/tabular treatment if cheap; no logic change.

#### 3.2 Selection isolation — remove permanent panels, route strictly
* Delete the always-mounted sections: `EditorInspector.svelte:1051-1067` (Camera Pan/Grid/Floor) and `:1069-1083` (Lighting Bright/Visitor + Ambient/Directional/Fog). Keep the `:1043-1048` empty-selection primer (or the P21 primer where it now lives) as the zero-selection state.
* Preserve existing routing (`:652` layout CAD / `:837` Arrange / `:896` assets / `:939` camera incl. `CameraPlanInspector` vs `EditorCameraInspector` / `:945` placement incl. Transform+Material/Primitive/Light). Camera node → pose/FOV/look-at/timing; connection → undirected endpoints + bidirectional duration + derived speed + spline type; 3D object → name + Transform + Placement + PBR material. Gate the `.place-tools` 2D drafting grid to Scene Plan Layout only — it never renders in Scene 3D or Camera.

#### 3.3 Relocation target — View menu, never status-bar controls
* Floor color, session ambient sliders, viewport grid toggles move into the existing **View menu** (`EditorViewportToolbar.svelte:170,385` owns the Scene 3D View menu; add Plan equivalents if missing). Status bar takes strings/hints only — authoring controls there violate its documented contract.

### Slice 4: Light-theme micro-tune + type grammar (porcelain-block scoped)

#### 4.1-4.2 Porcelain only
* Verify `porcelain-atelier` (`tokens.css:374-418`) against the reference PNGs: accent `#d94826`, soft 12%, borders `#ddd6cb/#c4bbb0`. Tune inside the block only if vibration persists (e.g. soften active-toggle fill toward 10% / deepen text toward sienna) — never touch `:root` navy or other dark palettes. Selection stays brand blue everywhere.
#### 4.3 Typography + tabular numerals
* Inspector grammar: section headers 11px/600/uppercase/`0.05em`/`text-muted`; labels 12px/400/`text-secondary`; values/inputs 12.5px/500/tabular/`text-primary` (current headers read `0.95rem/650` at `EditorInspector.svelte:1097` — align). Enable `tabular-nums` on all coordinates/dimensions/angles/timecodes (inputs + timeline ruler).

### Slice 5: Timeline — density only, P12 geometry frozen

* **Frozen:** collapsed **48px** mini-player, expanded header **36px**, transport **above the lanes**, one shared dock across Camera Plan/3D, scope/transport semantics verbatim. There is no red/coral border to strip (collapsed pill uses `--editor-border-normal` + shadow at `EditorCameraTimelineFrame.svelte:744-753`); do not re-introduce one, do not re-dock into the status bar, do not resize to 40px, do not swap the scope-capsule/edge-flip/transport/scrubber/POV-Observer composition (`:684-721`) for generic ⏮▶⏭ icons.
* **Allowed:** lane density per P21.1 (`label 120px, ruler 28px, lanes 44/48/34/34/32`), 11px tabular ruler timecodes with playhead pinned to current time, quiet ghost transport buttons, collapsed-pill polish (spacing, contrast, keyboard/focus) within the existing floating geometry. `+View Key` stays 3D-Sequence-only.

---

## 3. Corrected file map (replaces draft §3)

```text
apps/editor/src/lib/editor/
├── styles/
│   ├── tokens.css                        → verify 36/32/24 (.project-editor:510-513); porcelain-block-only tune; quiet hairlines
│   ├── controls.css                      → ghost/segmented grammar source (already exists; align, don't fork)
│   ├── editor-shell.css                  → 24px status enforcement (:26) + grid rows with EditorApp.svelte:2037
│   └── scene-palette.ts                  → single-end walker beside applyEditorGizmoPalette (:60-86)
├── theme.svelte.ts                       → registry only (draft path styles/theme.svelte.ts is wrong); no per-polish logic
├── app/
│   ├── ProjectRow.svelte                 → Row 1 verify 36px (:205), inner 28px (:208) [draft's app/EditorAppBar.svelte is wrong]
│   ├── WorkspaceRibbon.svelte            → Row 2 verify 32px (:60); unify segmented; 26px→28px button tune (:63)
│   ├── StatusBar.svelte                  → lock 24px standalone (reconcile :155-156 1.7rem); content already done
│   ├── PlanWorkspace.svelte              → shell only (draft path plan/PlanWorkspace.svelte is wrong); no banner work
│   └── CameraPlanWorkspace.svelte        → Camera Plan shell; no banner work
├── EditorInspector.svelte                → delete :1051-1083 permanent panels; gate .place-tools; keep routing
├── EditorTransformInspector.svelte       → keep .field-grid 3-col; fold axis legend into chips
├── fields/
│   ├── EditorNumberField.svelte          → compact 28px axis-chip variant + tabular-nums (NO new TransformInputRow)
│   └── EditorVec3Field.svelte            → same treatment where used
├── gizmo/
│   └── EditorTransformControlsHost.svelte → single-end walker call at :38-42 (sync init, picker untouched)
├── layout/
│   ├── LayoutPlanViewport.svelte         → remove .plan-help :1715-1728
│   └── LayoutDraftToolbar.svelte         → [Layout|Arrange] segmented unification (:64-67); keep Tour (:91)
├── camera-plan/
│   └── CameraPlanViewport.svelte         → remove .plan-help :808 (strings :161-173)
├── EditorViewportToolbar.svelte          → View menu relocation home (:170,385); ribbon re-host already done
└── camera/
    └── EditorCameraTimelineFrame.svelte  → density only; 48px/36px geometry frozen (:744-753, :782-789)
```

---

## 4. Verification checklist (Definition of Done)

* [ ] Rows measure 36 / 32 / 24 with no residual 44px bar; ribbon buttons 28px (or 26px kept by explicit QA note).
* [ ] All three switches (`Scene|Camera`, `Plan|3D`, `Layout|Arrange`) share one enclosed segmented grammar; no inactive button carries an accent outline.
* [ ] No `.plan-help` pill in either viewport; hints live only in the status-bar center zone.
* [ ] Camera Plan edges show one midpoint pill each (`4.0s`, or `4.2s · 3.8s` when directions differ; auto segments muted + dotted, never the word `auto`); selected edge pill is accent fill + white text. No node names or arrows on canvas.
* [ ] Gizmo: one arrowhead / grip-cube per axis tip; all axes present and draggable; hover/active/pickers intact; `three` bump re-check noted.
* [ ] Inspector: XYZ rows are single-line 28px with RGB chips + tabular nums; no permanent Camera/Lighting sections; no 2D Place tools outside Scene Plan Layout; empty state shows primer only.
* [ ] Viewport lighting/floor/grid live in View menu(s); status bar has zero authoring controls.
* [ ] Light-theme changes confined to the `porcelain-atelier` block; dark themes pixel-unchanged except shared grammar.
* [ ] Timeline: 48px collapsed / 36px header / transport-above-lanes preserved; ruler 11px tabular; `+View Key` 3D-Sequence only.
* [ ] Full Vitest + `check` + `build` (editor+museum) + `verify:visitor-bundle` (+ preview-surface closure gate) green; six-PNG comparison + axe/contrast/keyboard/reduced-motion sweep per P21 acceptance gate. No `/museum` chunk contains editor code (visitor isolation).

---

## 5. Draft §5 questions — answered (no implementer re-ask needed)

1. **Inspector decomposition:** one large `lib/editor/EditorInspector.svelte` (1247 lines) routing into `camera/EditorCameraInspector.svelte`, `app/CameraPlanInspector.svelte`, `EditorLight/Material/Placement/Primitive/TransformInspector` + `fields/*`. Permanent Camera/Lighting panels are inline sections (`:1051-1083`), not separate debug components — delete inline.
2. **Transform fields:** shared `fields/EditorNumberField.svelte` (props `label/value/step/min/fractionDigits/oncommit`, `<input type="number">` at `:80-89`) + `EditorVec3Field.svelte`. X/Y/Z are already shared-component rows in `EditorTransformInspector` — restyle, don't replace. No `NumericInput`, no `TransformInputRow` (do not create).
3. **Banner source:** neither draft candidate. `.plan-help` in `camera-plan/CameraPlanViewport.svelte:808` and `layout/LayoutPlanViewport.svelte:1715-1728`. No `instruction-banner` class exists.
